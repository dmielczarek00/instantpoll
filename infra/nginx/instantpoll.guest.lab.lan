server {
    listen 80;
    listen [::]:80;

    server_name instantpoll.guest.lab.lan;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name instantpoll.guest.lab.lan;

    ssl_certificate /etc/nginx/ssl/guest.lab.lan/instantpoll.guest.lab.lan.crt;
    ssl_certificate_key /etc/nginx/ssl/guest.lab.lan/instantpoll.guest.lab.lan.key;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    location / {
        proxy_pass http://192.168.1.108:3000;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
